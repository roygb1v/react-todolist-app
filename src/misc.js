class Options extends React.Component {
  // constructor(props) {
  //   super(props);
  //   // Bind returns a function bound to the current context it was defined in. So all subsequent handleRemoveAll calls call the function bound to the context it was defined.
  //   this.handleRemoveAll = this.handleRemoveAll.bind(this) // Binds to the constructor when creating the instance
  // }
  // // You pass this event handler as a callback in the react component
  // // However, the 'this' context is lost which is similar to reassignment
  // handleRemoveAll() {
  //   this.props.deleteAll();
  // }

  render() {
    
  }
}